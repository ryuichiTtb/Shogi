本リポジトリで作業する全 AI エージェント (Claude Code / Codex CLI / Cursor / Aider 等) が遵守するガバナンス・ルール。セッションやモデルが変わっても、記載ルールは**絶対の前提**として適用される。

---

<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

---

## 絶対ルール

### 1. PR作成・マージ・Issueクローズは明示指示まで禁止
PR作成・`gh pr merge`・`gh issue close` は、ユーザーの明示指示があるまで実行しない。コード変更完了後はブランチを push して止まり、確認を促す。「対応して」「Issue#XX を対応して」だけでは実行しない。**唯一の例外**: 「PR・マージ・クローズまでお願いします」と一連の作業として明示依頼された場合。

### 2. メイン作業は専用ブランチ / 軽微な派生修正は同居可
main への直接コミット・push は禁止。メイン Issue 対応は専用ブランチで行う。作業中に見つけた**軽微な派生修正** (1〜2 ファイル相当・独立してレビュー可・非アーキ変更) は、別 Issue を起こさず同ブランチに同居させ、メイン Issue のコメントに「派生で対応した内容」として記録する。**大規模** (アーキ変更・別レビュアー要・影響がメイン Issue と独立) は別 Issue + 別ブランチに分割。迷ったらユーザー確認。着手前に `git branch -a` で既存ブランチを確認し、対象 Issue 用ブランチがあれば用途をユーザーに確認してから切替える。**例外**: ガバナンス変更 (AGENTS.md / `.claude/settings.json` 等) は重要度が高く、専用 Issue + 専用ブランチで分離する。

### 3. ブランチ命名規則
形式は `{prefix}/#{Issue番号}` または `{prefix}/#{Issue番号}-{slug}`。

| prefix | 用途 |
|---|---|
| `feature/` | 新機能追加・機能拡張 |
| `fix/` | バグ修正 |
| `refactor/` | 振る舞いを変えない整理・責務分離・命名変更 |
| `chore/` | 雑務 (依存更新・設定変更・CI 調整等) |

1 Issue を複数 PR に段階分割する場合は slug (例 `cleanup`/`memo`/`compute`/`split`) で派生ブランチを切ってよい。prefix 判断に迷えばユーザー確認。Issue に紐づかない作業は Issue を先に作成する。

### 4. 新規ブランチは origin/main を起点にする
ブランチ作成前に必ず `git fetch origin`。`git checkout -b {prefix}/#{Issue番号} origin/main` で作る (またはローカル main を `git pull --ff-only origin main` してから切る)。ローカル main は他作業で古いままのことがあり、古い main から枝を切ると他 Issue の変更が欠けデグレに見える状態を招く。既存ブランチ切替時も `git fetch && git merge origin/main` で最新を取り込むかユーザー確認。

### 5. 破壊的操作・公開操作は事前確認
以下は必ずユーザー確認後に実行し、許可は当該 1 回限り (同種操作も都度確認)。

- **破壊的操作**: ファイル・ブランチ削除 (`rm -rf`, `git branch -D`)、`git reset --hard`、`git push --force`、DB 変更、プロセス kill、コミットの amend・履歴改変。※これらは `.claude/settings.json` で allow だが「明示指示があれば実行可」の設計であり、本ルール 5 (必ず確認してから実行) が**唯一の安全網**。明示指示なしの実行は違反。
- **公開・共有状態への影響**: `git push` (特に main・共有ブランチ。Issue 専用ブランチへの通常 push は確認済み範囲=ルール 6)、PR・Issue コメント、Slack・メール送信、外部サービスへのデータ送信。`git push origin main` / `gh issue close` / `gh pr close` は settings.json で auto-deny を維持。

### 6. 専用ブランチは push まで行う
実装・検証・コミットが完了したら、明示の「push しない」「止めて」指示がない限り、作業ブランチを `git push origin {branch}` して止まり、ブランチ名・コミット・実施した検証を報告する (Vercel プレビューでユーザーが確認するため)。main への直接 push は禁止。push 先が共有ブランチ / 用途が曖昧 / 未確認変更が混在する場合は push 前に確認。PR・マージ・クローズはルール 1 に従う。

### 7. コミット・PR・Issue運用
- **コミット**: 意味のある単位で分割 (機能追加とリファクタを混ぜない)。メッセージは日本語、「なぜ」重視で背景・原因・対策が第三者に伝わる粒度。フックはスキップ禁止 (`--no-verify` は明示指示時のみ)。
- **PR**: タイトル 70 文字以内。本文に Summary・Test plan・関連 Issue (`Closes #69`)。
- **Issue**: タイトルは簡潔に (目安 30 文字以内の名詞句)。詳細 (背景・目的・スコープ・受入条件) は本文。種別ラベル (`governance`/`bug`/`enhancement` 等) を付与。

### 8. 重要マイルストーンでの徹底レビュー
①実装計画策定直後 (着手前) ②実装完了時 (commit・push 前) ③マージ前 (PR レビュー) の 3 段階で、**Issue #109「共通レビュールール」を正本に**レビューする。観点は本ファイルに転記せず、各時点で `gh issue view 109` 等で最新を取得して用いる。レビューは AI 自身のセルフレビューを必須とし、必要に応じてユーザーレビューも行う (両軸)。指摘が全解消するまで次段階に進まない。結果は PR/Issue コメントか会話に明示的に残す。

### 9. Worktree運用 + 完了後の後始末
新規対応の着手前に、Worktree (`git worktree add`、既定配置 `.claude/worktrees/issue-{番号}`) で切り出すか、メインでブランチ切替かをユーザーに確認する (別件の未コミット変更が残る場合は Worktree 推奨)。対応完了 (PR マージ + Issue クローズ) 後は、使った Worktree・ローカルブランチ・origin ブランチを削除する。削除はルール 5 の対象なので、対象一覧 (Worktree path・ローカル/origin ブランチ名) を提示し確認を取ってから実行。

---

## 実装ガイドライン

絶対ルールが禁則を扱うのに対し、本節は実装の**品質基準・進め方・報告形式**を定める。

### 1. 成功条件 (完了の定義)
全変更が以下を**同時に**満たすことを目指す: 要件充足 / 既存機能を壊さない (テスト緑) / PC・モバイル双方で UI・UX 最適化 / モバイルの多様な画面サイズ・アスペクト比に耐える共通基盤 / 性能・保守性・セキュリティ・デグレ観点で問題なし / 類似バグ・対応漏れ・潜在的な穴なし / デッドコード・不要コード・マジックナンバー・過剰ハードコーディングなし / 計算量を意識した高パフォーマンス / リッチ機能でもラグ・ズレ・発熱・電池消耗を最小化。完了報告は本節 9 のテンプレで各項目の確認結果を示す (未充足は理由と次アクションを明示)。

### 2. 技術スタック
TypeScript / Next.js / React、Prisma / PostgreSQL (Neon)、Vercel、pnpm (`pnpm-lock.yaml` 維持)、単一リポジトリ。依存追加・更新は `pnpm add` / `pnpm update`。パッケージ追加・外部 API 呼び出し等スタックに影響する変更は事前確認 (本節 7)。

### 3. 実装方針
関連ソースを広く読み全体像を把握してから設計・実装する。優先度: **パフォーマンス >= 保守性 > 可読性**。ただし最重要は UX 体験 (ラグ・ズレ・端末負荷・発熱・バッテリー消耗の抑制)。冗長化を避け一元化・一般化・共通化と疎結合を重視し、同じ挙動なら計算量・描画負荷・メモリ・OS リソース消費が小さい設計を選ぶ。マジックナンバー・過剰ハードコーディング・不要分岐・デッドコードを残さない。可読性のために分岐/抽象化を増やす際は性能影響を必ず確認。

### 4. UI / UX方針
PC・モバイル双方で最適化。モバイルは縦横・アスペクト比・性能差が大きいため単一端末前提にしない。小幅 (320px〜) と大画面の双方で破綻しないレスポンシブ設計とする。見た目だけでなく操作感・待ち時間・描画負荷・アニメの滑らかさも UX 品質。リッチ演出 (アニメ・音声) 追加時は端末負荷・電池・発熱・遅延への影響を考慮し、低スペック端末向けの軽量フォールバックを設計に含める。

### 5. 意思決定優先順位
迷ったら順に決める: ①UX 品質 (ラグ・ズレ・端末負荷) ②パフォーマンス (計算量・描画負荷・メモリ) ③保守性 (共通化・疎結合・再利用) ④既存仕様との整合 (デグレ回避) ⑤可読性。下位を優先する時は上位に悪影響がないことを根拠付きで示す。

### 6. 必須チェック (ソース変更時)
この順で実行: ①`npm run lint` ②`npm run typecheck` ③`npm run test:ci` (`test` は watch モードのため自動チェックには `test:ci`) ④`npm run build`。失敗は**通るまで修正してから報告** (解消不能なら原因・影響・次案を明記)。ドキュメントのみの変更は lint / typecheck の確認で代替してよい (build 省略可)。

### 7. 外部通信・機密情報ポリシー
- **外部通信**: パッケージ追加・API アクセス・Web 検索・外部サービス操作は原則禁止・都度確認。やむを得ず行う場合は目的・通信先・送受信データ・代替案を提示して承認を得る (依存追加は必要性・代替手段・サイズ・ライセンスも示す)。
- **機密情報**: `.env*`・鍵情報は読み取り/出力/コミット禁止。ログ・報告にトークン・接続文字列 (Neon URL 含む) を出力しない。`.env` はキー存在など構造確認のみ。機密値が要る作業はダミー値+手順提示で対応。

### 8. コミュニケーション方針
日本語のみ。報告は理由・代替案・懸念点・影響範囲を含め詳細に。不明点は推測で進めず、着手前に「前提・想定動作・不明点」を明示してユーザー合意を得てから進める。途中で前提が崩れたら止めて再確認する。

### 9. 最終報告テンプレート
実装完了報告は原則この構成に揃える (軽微な変更は省略可。該当セクションが空なら「該当なし」と明記):

**Summary** / **変更内容** (ファイル別・対応要件) / **設計判断** (理由・代替案比較・性能/保守性/UX 観点) / **テスト結果** (実行コマンドと結果、失敗は原因・影響・対応案) / **パフォーマンス・UX 観点の確認** (計算量・描画・メモリ・モバイル負荷・ラグ/ズレ/発熱/電池・PC/モバイル双方) / **残課題・次アクション**。

### 10. 追加禁止事項
不明点を推測だけで進める / 根本原因を特定せず症状だけ消す場当たり修正 / デグレ確認なしで完了扱いにする / 不要コード・デッドコード・マジックナンバー・過剰ハードコーディングを残す。

---

## カード将棋: 新規カード追加時のチェックリスト

カード将棋 (card-shogi variant) に新カードを `CARD_DEFS` へ追加する際の横断制約チェックリスト (待った・DB 保存・AI ガード・王手中使用可否等) は **[docs/card-shogi-new-card-checklist.md](docs/card-shogi-new-card-checklist.md)** を参照。新カード追加作業時に必ず目を通すこと。

---

## ルールの追加・更新フロー

本ファイルの更新手順: ①「governance」ラベルで Issue 化 (追加/変更するルールと理由を記載) → ②`feature/#{Issue番号}` または `chore/#{Issue番号}` で対応 → ③ルール本文と簡潔な理由をセットで記述 → ④通常フローで PR 作成・レビュー。

各ルールは**本文 (何をすべき/すべきでないか) と簡潔な理由**を必ず備える (将来のエッジケース判断に理由が要るため)。具体的な手順・適用範囲・例外は必要に応じて併記する。
